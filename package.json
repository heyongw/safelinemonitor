{
  "name": "leichimonitor",
  "version": "1.0.0",
  "private": true,
  "description": "多台雷池(SafeLine) WAF 监控指标汇总看板",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/collector.js && node --check src/leichi.js && node --check src/config.js && node --check src/httpclient.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
